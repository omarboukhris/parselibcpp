%import "ext/body.grm"

%import "ext/tokens.grm"

AXIOM -> file

file -> 
	!imports !classdef

imports ->
	"import" !importfile |
[] // list operator~~~ classes classes | eps('')

importfile -> !identifier. ".inc" semic.

classdef ->
	"class" classname=identifier. lcrch. !classbody rcrch. semic. |
[]